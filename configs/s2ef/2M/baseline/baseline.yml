includes:
  - configs/s2ef/2M/base.yml

model:
  name: baseline

optim:
  batch_size: 500
  eval_batch_size: 500
  num_workers: 8
  lr_initial: 0.0008
  optimizer: AdamW
  optimizer_params: {"amsgrad": True}
  eval_every: 5000
  max_epochs: 12
  clip_grad_norm: 20
  ema_decay: 0.999
